version: '3'

services:
  db:
    image: bitnami/mysql
    ports:
      - 3306:3306
    environment:
      - MYSQL_ROOT_USER=JoaoPedroAFLuz
      - MYSQL_ROOT_PASSWORD=docker
      - MYSQL_DATABASE=devfood